import 'package:flutter/material.dart';

class TmpSendMailScreen extends StatelessWidget {
  const TmpSendMailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("발송 완료!")),
    );
  }
}
