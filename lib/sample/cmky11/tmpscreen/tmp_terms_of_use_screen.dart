import 'package:flutter/material.dart';

class TmpTermsOfUseScreen extends StatelessWidget {
  const TmpTermsOfUseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("서비스 이용약관")),
    );
  }
}
