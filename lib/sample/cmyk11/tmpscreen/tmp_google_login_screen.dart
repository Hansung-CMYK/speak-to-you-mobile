import 'package:flutter/material.dart';

class TmpGoogleLoginScreen extends StatelessWidget {
  const TmpGoogleLoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Google 로그인")),
    );
  }
}
