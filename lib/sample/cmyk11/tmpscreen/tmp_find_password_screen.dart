import 'package:flutter/material.dart';

class TmpFindPasswordScreen extends StatelessWidget {
  const TmpFindPasswordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("비밀번호 찾기")),
    );
  }
}
