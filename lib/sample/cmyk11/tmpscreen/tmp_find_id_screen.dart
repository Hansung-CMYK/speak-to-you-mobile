import 'package:flutter/material.dart';

class TmpFindIdScreen extends StatelessWidget {
  const TmpFindIdScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("아이디 찾기")),
    );
  }
}
