import 'package:flutter/material.dart';

class TmpHomeScreen extends StatelessWidget {
  const TmpHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("홈 화면")),
    );
  }
}
