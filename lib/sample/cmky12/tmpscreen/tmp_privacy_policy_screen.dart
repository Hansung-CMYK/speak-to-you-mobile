import 'package:flutter/material.dart';

class TmpPrivacyPolicyScreen extends StatelessWidget {
  const TmpPrivacyPolicyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("개인정보 처리방침")),
    );
  }
}
